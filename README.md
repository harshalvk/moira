# Moira

A Kubernetes scheduler built from first principles — starting as a naive
placement loop and progressing to gang scheduling and tenant-fair queueing,
implemented as a real scheduler-framework plugin (drop-in, not a fork).

See `docs/adr/` for design decisions.

## Status

Phase 0, Step 1 — project scaffold only. No scheduling logic yet.
